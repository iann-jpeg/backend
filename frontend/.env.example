# Frontend environment example for Galloways
# Set the API base URL used by the frontend (must include /api if backend serves under /api)
VITE_API_BASE_URL="http://localhost:8000/api"

# When deploying to production (cPanel) change to your backend public URL. For your setup:
# Point to your site domain; if you host backend under the same domain use the /api path:
# VITE_API_BASE_URL="https://galloways.co.ke/api"

# Optional: enable a simple dev proxy (handled in vite.config) - leave false unless configured
VITE_USE_PROXY="false"

# NOTE: Vite only exposes variables prefixed with VITE_. Set these before running `npm run build`.
